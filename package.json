{
  "name": "enterprise-calculator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:compatibility": "playwright test",
    "test:enterprise": "node --test \"tests/enterprise/**/*.test.js\""
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^8.2.2"
  }
}
